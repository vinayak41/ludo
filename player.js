class Player {
  constructor(number, name, color) {
    this._position = [0, 0];
    this._name = name;
    this._color = color;
    this._number = number;
  }

  set position([x, y]) {
    this.
  }
}
